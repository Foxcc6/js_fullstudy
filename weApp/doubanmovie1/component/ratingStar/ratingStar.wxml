<!--component/ratingStar/ratingStar.wxml-->
<view class="star-wrapper">
    <block wx:for="{{list}}"wx:key="index">
        <image class="image" src="{{item}}" style="width:{{iconSize}}; height:{{iconSize}}" >
            
        </image>
    </block>
</view>
